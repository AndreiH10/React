import './buttons.css';

function Buttons(){
    return(
        <div>
            <button className="buttons">Login</button>
            <button className="buttons">Sign up</button>
        </div>
    )
}

export default Buttons